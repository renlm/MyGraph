spring:
  application:
    name: mygraph
  profiles:
    active: test
  cloud:
    config:
      failFast: true
      name: crawler,scripts,hljggzyjyw
      uri: ${CONFIG_URI:http://localhost:7000}
      username: ${CONFIG_USERNAME:springCloud}
      password: ${CONFIG_PASSWORD:123654}