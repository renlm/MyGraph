spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}
  main:
    allowCircularReferences: true
  cloud:
    kubernetes:
      reload:
        enabled: true
      client:
        namespace: renlm
      config:
        name: mygraph-configmap
        namespace: renlm