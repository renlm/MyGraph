spring:
  profiles:
    active: prod
  main:
    allowCircularReferences: true
  cloud:
    kubernetes:
      reload:
        enabled: true
      client:
        namespace: renlm
      config:
        name: mygraph-configmap
        namespace: renlm