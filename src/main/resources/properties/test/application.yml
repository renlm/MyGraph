server:
  port: 80
    
logging:
  level:
    root: trace
    cn.renlm.graph: debug
  file:
    name: /opt/log/my-graph.log
    max-size: 10MB
    max-history: 7
    
mybatis-plus:
  configuration:
    localCacheScope: STATEMENT
    cacheEnabled: true
  global-config:
    db-config:
      update-strategy: IGNORED

spring:
  profiles:
    active: test
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  thymeleaf:
    cache: false
  jackson: 
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: true
  datasource:
    dynamic:
      primary: master
      strict: true
      datasource:
        master:
          url: jdbc:postgresql://renlm.cn:5432/renlm
          username: renlm
          password: K86erp7IMYust^tn50jlsdi^_fyw4bnmdf4K
          driver-class-name: org.postgresql.Driver
          init:
            schema: classpath:schema/PostgreSQL/schema.sql
            data: classpath:schema/PostgreSQL/data.sql
            continue-on-error: false
            separator: ";"