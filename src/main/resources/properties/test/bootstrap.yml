spring:
  profiles:
    active: test
  main:
    allowCircularReferences: true
  cloud:
    kubernetes:
      reload:
        enabled: true
      client:
        namespace: renlm
      config:
        name: mygraph-configmap
        namespace: renlm