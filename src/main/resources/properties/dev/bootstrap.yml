spring:
  application:
    name: ${SPRING_APPLICATION_NAME:mygraph}
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  lifecycle:
    timeoutPerShutdownPhase: 30s
  main:
    cloudPlatform: KUBERNETES
    allowCircularReferences: true
  cloud:
    kubernetes:
      reload:
        enabled: true
      client:
        namespace: renlm
        masterUrl: https://rancher.renlm.cn/k8s/clusters/c-m-tpcnvmsr
        oauthToken: kubeconfig-user-z56jcdqshq:cmzdxlld9wln9t29bpw6stsk4fsk4mqqgvpw6ld48mbmtdj4wggkfw
      config:
        name: mygraph-configmap
        namespace: renlm