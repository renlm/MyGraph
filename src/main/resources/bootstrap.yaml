# -DDB_INIT_MODE={ALWAYS|NEVER}
# -DSECURITY_USER_NAME={default}
# -DSECURITY_USER_PASSWORD={123654}
# -DCONFIGSERVER={https://config.renlm.cn}
spring:
  application:
    name: mygraph
  main:
    allowCircularReferences: true
  profiles:
    active: ${PROFILES_ACTIVE:dev}
  cloud:
    config:
      uri: ${CONFIGSERVER:https://config.renlm.cn}
      username: ${SECURITY_USER_NAME:default}
      password: ${SECURITY_USER_PASSWORD:123654}
      name: mygraph,logging,actuator
      profile: ${spring.profiles.active}
      label: master
      failFast: true